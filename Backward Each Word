# In a given string you should reverse every word, but the words should stay in their places. 

def backward_string_by_word(text: str) -> str:
    words = text.split(' ')
    result = ' '.join(reversed(words))[::-1]
    return result
    
